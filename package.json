{
  "name": "miniki-fashion",
  "version": "1.0.0",
  "private": true,
  "description": "MINIKI FASHION - Designer Boutique | Maternity & New Born Shop - Full Stack E-Commerce Platform",
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client && npm install --prefix admin",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "admin": "npm run dev --prefix admin",
    "dev": "concurrently \"npm run server\" \"npm run client\" \"npm run admin\"",
    "build:client": "npm run build --prefix client",
    "build:admin": "npm run build --prefix admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
